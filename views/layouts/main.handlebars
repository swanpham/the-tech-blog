<!DOCTYPE html>

<html lang="en">
    
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>THE TECH BLOG</title>
            <link rel="stylesheet" href="/stylesheets/style.css">
        </head>

        <body >
            <div class="wrapper ">
                <header>
                    <h1>
                        <a href="/">THE TECH BLOG</a>
                    </h1>
                    <nav >
                        {{#if loggedIn}}
                        <a class="singup-login" href="/dashboard">dashboard</a>
                        
                        <button id="logout" class="btn-no-style singup-login">logout</button>  
                                      
                        {{else}}
                        <a class="singup-login" href="/login">Sigup/login</a>
                        {{/if}}

                        
                        
                    </nav>
                </header>
                <main>
                    {{{ body }}}
                </main>
                <footer>
                    Thanks for visiting!
                </footer>
            </div>
        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
        {{/if}}
        </body>

    

</html>